# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Node
node_modules/
.next/
out/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
.coverage
htmlcov/
.pytest_cache/
test-results/
playwright-report/
.lighthouseci/
blob-report/

# Evals — run artifacts under evals/results/ ARE committed (they're the accuracy
# history), and so is evals/ground_truth.json: CI gates on `score --offline`, which
# can't reach EDGAR, so a gitignored pin would be no pin at all on a fresh checkout.
# .cache/ is the pre-pin layout, kept ignored so stale local copies stay out of diffs.
backend/evals/.cache/
# The Q&A corpus is the exception: it is a `run` input only. The gate reads the
# artifact, which already carries every chunk the model saw, so committing ~7 MB of
# float vectors CI never opens would buy nothing. Rebuild with `eval_qa build-corpus`.
backend/evals/qa_corpus/

# Planning
docs/
tasks/
.claude/
CLAUDE.md
AGENT.md

# Roadmap 11.3: the OpenAPI dump and the types generated from it. `npm run contract`
# rebuilds all three; none is ever imported by application code.
frontend/openapi.json
frontend/src/lib/contract/generated.ts
frontend/src/lib/contract/assert.ts
